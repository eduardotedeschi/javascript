const fruta = 'Abacaxi';

for(let valor of fruta){
    console.log(valor);
}

const nomes = ['Eduardo', 'Gislene', 'Roberto'];

for (let valor of nomes){
    console.log(valor);
}

nomes.forEach( (elementos) => {
    console.log(elementos);
});